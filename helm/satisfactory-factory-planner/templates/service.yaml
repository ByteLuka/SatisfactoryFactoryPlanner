apiVersion: v1
kind: Service
metadata:
  name: {{ include "sfp.fullname" . }}
  labels:
    {{- include "sfp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sfp.selectorLabels" . | nindent 4 }}
